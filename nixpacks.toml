[phases.setup]
nixPkgs = ["nodejs_20"]

[phases.install]
cmds = ["npm ci"]

[phases.build]
cmds = ["npm run db:generate", "npm run build --workspace=@uln/web"]

[start]
cmd = "npm run start --workspace=@uln/web"
